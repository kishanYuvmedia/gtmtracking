'use client';

import { Card } from '../../components/ui/Card';

export function EventChart() {
  return (
    <Card title="Events Over Time">
      <div className="h-64 flex items-center justify-center text-gray-400">
        <p>Chart will render here (recharts)</p>
      </div>
    </Card>
  );
}
